name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Compile
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
      - run: go version
